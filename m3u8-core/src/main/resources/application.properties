# Maximum number of concurrent download threads (not used in virtual thread mode)
download.max-threads=10

# Maximum number of retry attempts for failed downloads
# Increased from 3 to 5 to better handle rate limiting with exponential backoff
download.max-retries=5

# HTTP request timeout in milliseconds (5 minutes)
download.timeout=300000